#include "main.h"

/**
 * int _islower - checks for lowercase alphabet
 *
 * Description: A C program that checks for lowercase alphabet
 *
 * Return: Always 0 (Success)
*/

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
